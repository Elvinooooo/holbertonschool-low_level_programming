#include "main.h"
/**
*_isupper: Checks for uppercase character
*@c: the argument of the function to check
*
*Return: 0 if the character is uppercase 1 if is lowercase 
*/

int _isupper(int c)
{
	if(int isupper(int c))
	{
		return (1);
	}
	else
	{
		return (0)
	}
}
