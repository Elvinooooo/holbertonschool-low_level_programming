#include "main.h"
/**
*mul - multiplies the two given integers
*@a: first argument for the multiplication
*@b: the second integer for the multiplication
*
*Return: result
*/

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
