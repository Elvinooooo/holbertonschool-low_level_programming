#include "main.h"
/**
* add - Function that adds two integer
*
* @a: the first parameter;
* @b: the second parameter;
*
* Return: returns the result of addition of two integers
*/

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);

}
